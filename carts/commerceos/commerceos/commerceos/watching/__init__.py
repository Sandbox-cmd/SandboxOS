"""the watching — standing attention over the business, both directions.

engine.evaluate() runs a store's watch-list (config rows, never code)
over landed facts; findings.py holds the records and their lifecycle —
evidence or the write is refused; status.py fills the part's registry
row. spec: ~/Sandbox/channels/commerceos/spec/parts/watching.md.
"""
