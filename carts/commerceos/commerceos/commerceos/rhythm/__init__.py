"""the standing rhythm — owner-armed cadence + push (notify-only).

runner.py ticks the parts on their clocks; notify.py is the ntfy wire;
arm.py holds the only arming path (the owner's --yes); status.py is the
part's registry row. nothing here arms itself (spec/build.md standing
constraints): the schedule exists only after the owner's own keystroke.
"""
