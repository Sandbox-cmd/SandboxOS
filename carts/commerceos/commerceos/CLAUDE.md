@AGENTS.md

claude-specific: this project has a channel in the owner's workshop —
its landed memory is `channels/<channel-name>/CANON.md` there. before
deciding anything twice: `sandbox recall QUERY --channel <channel-name>`.
findings and decisions: `sandbox note "…"` (stages; the owner lands).
