# frame — the fleet roster (agents, skills, agent-scoped skills)

state: frame draft — 2026-07-18, for sitting A. nothing here is ruled;
the owner converges from these sketches.

the law every candidate obeys: it names the job row it serves and the
writer-class it holds, or it is cut. one writer per field-class. autonomy
starts narrow and widens only on evidence. findings and proposals always
carry provenance.

## agents — workers inside commerceos

**in this run (already scoped):**

| agent | serves | writer-class | posture |
|---|---|---|---|
| catalog (built) | O3 | product fields + publish state | reversible acts; fit-critical parks |
| analyst | O1, O6 | findings only — no store writes | hunts on cadence, mints through the one findings door |
| content/seo | O3, C2 | titles, meta, feed text | reversible content acts; customer-facing claims park |

**candidates for the backlog (build later, each on a real need):**

| candidate | serves | writer-class | why it earns a slot | why it waits |
|---|---|---|---|---|
| merchandiser | O3, C1 | collection membership | CW4/CW5 build the executor; an agent can then propose curation on sales evidence | executor first; evidence second |
| images | O3, C2 | product media | the biggest catalog gap (0% real images) | gated on the CW9 sourcing ruling |
| localization (Arabic) | C2 | translations | Arabic as real as English is a ruled customer promise | needs a source + human-confirm ruling before it may run |
| supplier clerk | O5 | supplier + purchase-order facts | the spine's supplier/PO tables are empty; someone must land those facts | shape depends on sitting D (books import vs gated form) |
| bookkeeper (monthly close) | O5 | none — reports only | runs reconciliation on cadence, drafts the close; the owner reads and lands | cheap; can enter soon after the analyst proves the pattern |

**deliberately not agents:** pricing (writer-class held by nobody — proposals
only, per spec), forecaster (reserved, unearned), concierge (separate
substrate, phase 2, never the personal subscription).

## skills — repeatable ceremonies the operator (or an agent) invokes

| skill | what it does | rides on |
|---|---|---|
| store-onboard | walks a new store into `stores/<name>/` — config set, DB, first sync, first render | lane M (M4 is its first run) |
| monthly-close | books import → reconcile → economics report → staged summary | economics engine (built) |
| catalog-batch | arm a feature batch from the terminal when the web is not to hand | workflow engine (built) |

## agent-scoped skills — playbooks that live with one agent's manifest

- the analyst's hunt playbooks (one per hunt: correlations, cohort shifts,
  channel drift) — so a hunt is a written, repeatable method, not a vibe.
- the content agent's voice guide (plain words, claims cite the catalog,
  never re-derive a spec value) — the guard test enforces the floor; the
  skill carries the craft.

## the recommendation (for sitting A)

Build in this run: analyst + content/seo only (the run's cap). Everything
else enters the backlog as a row with job row + writer-class + check, pulled
when its dependency lands. Skills: store-onboard rides lane M this run; the
other two are backlog rows.
